# happenings
Happenings is a full-stack web application that allows users to discover, create, and manage social events. It is built using Java Servlets, JSP, and a MySQL database. Users can browse events and interact with event data through a web interface. The application is deployed as a web-based system accessible through a browser.
